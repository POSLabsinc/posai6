

## Plan: Increase Apply Button Width

**Change**: In `src/components/DiscountDialog.tsx` at line 370, increase the horizontal padding from `px-8` to `px-14` (or similar) to make the Apply button slightly wider.

**File**: `src/components/DiscountDialog.tsx` (line 370)
- Change `className="px-8 py-2.5 rounded-[10px] text-sm font-medium transition-all"` → `className="px-14 py-2.5 rounded-[10px] text-sm font-medium transition-all"`

