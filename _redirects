# Cloudflare Pages Redirects

# API routes are handled by Pages Functions (no redirect needed)
# Functions in /functions/api/* automatically map to /api/*

# SPA fallback - serves index.html for all non-file routes
# This must be last
/*  /index.html  200
